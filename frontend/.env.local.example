# Frontend env — copy to .env.local. Vercel reads the same names from
# project env vars in production.

NEXT_PUBLIC_API_URL=http://localhost:8000

# URL-safe base64 from `vapid --applicationServerKey`. Without it,
# EnableNotificationsButton hides itself (push is opt-in only).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
