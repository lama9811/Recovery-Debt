# Slim production deps — only what the OAuth + DB layer needs today.
# The ML pipeline (scikit-learn, shap, scipy, pandas, numpy, numba, llvmlite)
# is preserved in `requirements-full.txt` and will be merged back in on Day 8.
asyncpg==0.31.0
fastapi==0.136.1
httpx==0.28.1
python-dotenv==1.2.2
uvicorn[standard]==0.46.0
