DATABASE_URL=postgresql://postgres:xxx@db.xxxxxxxxxxx.supabase.co:5432/postgres
SUPABASE_JWT_SECRET=

# WHOOP OAuth — register the app at https://developer.whoop.com.
# Set WHOOP_REDIRECT_URI in the WHOOP developer portal to match this exactly.
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:8000/api/whoop/callback
WHOOP_WEBHOOK_SECRET=

# Where the backend redirects the user after a successful WHOOP connect.
FRONTEND_URL=http://localhost:3000

ANTHROPIC_API_KEY=

# Web Push (Day 15). Generate with `pip install py-vapid && vapid --gen`.
# The matching public key goes in the frontend env as
# NEXT_PUBLIC_VAPID_PUBLIC_KEY. VAPID_SUBJECT must be a mailto: or https:
# URL so push services can reach you on bounce.
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

USER_TIMEZONE=America/New_York
